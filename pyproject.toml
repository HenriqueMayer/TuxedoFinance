[project]
name = "django-finance-template"
version = "0.1.0"
description = "CashFlow — a self-hosted personal finance tracker built with Django, server-rendered and zero-JS."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7,<6.1",
]
